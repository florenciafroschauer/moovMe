package model;

import java.util.List;

public interface ScoreI {
    void addClient(List<ClientI> client);
    // List<Nickname> showBestClients();
    // List<ClientI> score(arrayList <ClientI> clients);
    // Discount makeDiscount(ClientI aClient, Asset asset);
}
